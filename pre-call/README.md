# Pre-call (closewithcjclay.com)

Three HTML files matter; two are **different products**:

| File | What it is |
|------|------------|
| `HTSA-pre-call-resources-universal.html` | **Live “Before Our Call” URL** — shorter **slim** flow: same **“Show up dangerous · Not confused”** hero, watch list, progress ring, basic prep. **No** industry ranker, **no** full income / pain / compare / voice blocks. |
| `HTSA-pre-call-resources-universal-slim.html` | Frozen copy of that **slim** universal (backup; safe to keep). |
| `HTSA-pre-call-resources-filter.html` | **Codex engagement / phased game-plan** — different layout and JS (`htsa-engagement-tool-v2`). Not the same page as the long Cursor “Show Up Dangerous” single-file build. |

### Full Cursor build (industry chips, income math, pain, compare, voice, Formspree)

That **full** single HTML file (with `INDUSTRY_BY_CAT`, `htsa-path-fields-v1`, `htsa-precall-filter-v1` for checkboxes, etc.) **has not been in git** — only the slim universal and the filter page are. If you still have that file in chat or Codex, save it into this folder as e.g. `HTSA-pre-call-show-up-dangerous-full.html`, then either replace `universal.html` or swap the main link after review. Rename the progress `storageKey` in that paste to something like `htsa-precall-universal-full-v1` so it does not collide with any other page’s saved checkboxes.

**Links**

- https://closewithcjclay.com/pre-call/HTSA-pre-call-resources-universal.html  
- https://closewithcjclay.com/pre-call/HTSA-pre-call-resources-filter.html  

`Snapshots/` — frozen copies when importing from Codex (`*-codex-snapshot.html`). Safe to ignore day-to-day; delete later if you want less clutter in the folder listing.
