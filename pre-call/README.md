# Pre-call & related HTSA HTML (closewithcjclay.com)

| File | Purpose |
|------|---------|
| `HTSA-pre-call-resources-universal.html` | Original universal pre-call resources page. **Dark theme only** (no OS light-mode flip). |
| `HTSA-pre-call-resources-filter.html` | **Engagement / game-plan flow** (Codex sequence). **Canonical colored UI** — palette matches universal + invoices. Live: `/pre-call/HTSA-pre-call-resources-filter.html` |
| `snapshots/` | Frozen copies named `*-codex-snapshot.html` so a Git revert never loses the import. |

**Snapshots** duplicate the Codex exports from your Mac at import time. Edit the non-snapshot files for ongoing work; re-copy into `snapshots/` only when you want a new frozen backup.
