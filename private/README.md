# Private share links (unlisted)

Files here are on the live site so CJ can share a URL, but they are **not linked** from the homepage and are blocked from search crawlers via `robots.txt` + `noindex`.

**Important:** GitHub Pages is still a public host. Anyone who gets the exact URL can open the page. This is “unlisted,” not password-locked.

Do not add links to `/private/` from public nav or marketing pages.
